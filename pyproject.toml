[project]
name = "ai-commanders"
version = "0.1.0"
description = "Terra Invicta-inspired space battle simulator with LLM-controlled captains"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "httpx>=0.24.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
mcp = [
    "mcp>=1.2.0",
    "aiohttp>=3.9.0",
    "httpx>=0.27.0",
]
viz = [
    "matplotlib>=3.10.9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pillow>=12.3.0",
    "playwright>=1.62.0",
    "pytest>=7.0.0",
    "scipy>=1.15.3",
]
